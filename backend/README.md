# IdeaLens backend

IdeaLens is an evidence-driven technical idea intelligence engine. This is the
**M1 engineering foundation**: a runnable FastAPI skeleton with configuration,
database/Redis infrastructure boundaries, and a health endpoint. No product
functionality (document parsing, retrieval, evidence, analysis, reports,
workers) is implemented yet.

See the repository root `README.md` for setup, commands, and scope.